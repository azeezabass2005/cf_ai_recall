package com.recall

import android.app.Application

/**
 * Application class. DI wiring will land in milestone §4 — for the scaffold
 * we just exist as the manifest's `android:name` target.
 */
class RecallApp : Application()
